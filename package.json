{
  "name": "http-middleware",
  "version": "1.0.5",
  "description": "Middleware layer for http",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/http-middleware",
  "keywords": [
    "middleware",
    "http",
    "mw",
    "minimal",
    "request",
    "response",
    "vanilla"
  ],
  "license": "MIT",
  "dependencies": {
    "dezalgo": "^1.0.3",
    "is-incoming-message": "^1.0.0",
    "is-server-response": "^1.0.0",
    "map-limit": "0.0.1",
    "noop2": "^2.0.0"
  },
  "devDependencies": {
    "dependency-check": "^2.5.0",
    "get-server-port": "^1.0.0",
    "istanbul": "^0.3.18",
    "nets": "^3.1.0",
    "standard": "^5.1.1",
    "tape": "^4.2.0"
  },
  "files": [
    "LICENSE",
    "index.js",
    "README.md"
  ]
}
